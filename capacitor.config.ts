import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.syntaxelab.app",
  appName: "SyntaxeLab",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
